<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yb.dao.NotOilTradeDao">

	<select id="query" resultType="NotOilTrade">
		select 
		<if test="date=='day'">
			DATE_FORMAT(date, '%Y-%m-%d')	
		</if>
		<if test="date=='month'">
			DATE_FORMAT(date, '%Y-%m')	
		</if>
		<if test="date=='year'">
			DATE_FORMAT(date, '%Y')	
		</if> as date,sum(tradeNumber) tradeNumber,sum(tradeAmount) tradeAmount,sum(tradeAmount)/sum(tradeNumber) as singleAmount
		
		from res_notOilTrade
		
		where  
		<if test="station!='all'">
			oilStation=#{station} and
		</if>
		 date>#{start} and date &lt; #{end}
		 
		 group by 1
	</select>
</mapper>
